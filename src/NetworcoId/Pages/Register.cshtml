@page
@model NetworcoId.Pages.RegisterModel
@{
    ViewData["Title"] = "Opprett konto";
}

@section Styles {
    <style>
        [x-cloak] { display: none !important; }
    </style>
}

<div class="min-h-screen flex items-center justify-center p-4">
    <!-- Theme Toggle -->
    <div class="fixed top-6 right-6">
        <partial name="_ThemeToggle" />
    </div>

    <div class="bg-white dark:bg-gray-800 rounded-3xl shadow-sm border border-networco-border dark:border-gray-700 max-w-md w-full p-10 transition-colors duration-300">
        <div class="text-center mb-10">
            <h1 class="font-heading text-3xl font-black text-networco-authority-dark dark:text-white tracking-tight mb-2">NETWORCO</h1>
            <p class="text-networco-authority-light dark:text-gray-400 font-medium">Opprett din konto for å komme i gang</p>
        </div>

        @if (Model.RegistrationSuccess)
        {
            <div class="bg-green-50 dark:bg-green-900/20 border border-green-100 dark:border-green-800/30 rounded-2xl p-8 mb-6 text-center">
                <div class="flex flex-col items-center gap-4">
                    <div class="w-16 h-16 bg-green-100 dark:bg-green-900/40 text-green-600 dark:text-green-400 rounded-full flex items-center justify-center mb-2">
                        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"></path>
                        </svg>
                    </div>
                    <h3 class="font-heading text-xl font-bold text-networco-authority-dark dark:text-white">Sjekk e-posten din!</h3>
                    <p class="text-networco-text dark:text-gray-400 font-medium leading-relaxed">
                        Vi har sendt en bekreftelseslenke til din e-postadresse. Klikk på lenken for å aktivere kontoen din.
                    </p>
                    <p class="text-xs text-networco-authority-light/80 font-bold uppercase tracking-wider mt-4">
                        Ser du den ikke? Sjekk søppelposten.
                    </p>
                </div>
            </div>
        }
        else
        {
            @if (!string.IsNullOrEmpty(Model.ErrorMessage))
            {
                <div class="bg-red-50 dark:bg-red-900/20 border border-red-100 dark:border-red-800/30 text-red-700 dark:text-red-400 rounded-2xl p-4 mb-8 text-center text-sm font-medium">
                    <p>@Model.ErrorMessage</p>
                </div>
            }

            @* Vipps registration is intentionally hidden until implemented. *@
            @* BankID registration is intentionally hidden here (moved to youth
               registration, see issue #104). IDURA_ENABLED still controls the IDura
               integration itself (verify-to-link), only this button is removed here. *@

            <form method="post" id="registerForm" class="space-y-6">
                @Html.AntiForgeryToken()
                <input type="hidden" name="return_url" value="@Model.ReturnUrl" />
                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label for="firstName" class="block text-xs font-bold text-networco-authority-light/80 uppercase tracking-widest mb-2 ml-1">Fornavn</label>
                        <input type="text" id="firstName" name="firstName" required
                            class="w-full px-5 py-4 bg-networco-baby-50 dark:bg-gray-900 border border-networco-border dark:border-gray-700 rounded-2xl focus:ring-4 focus:ring-networco-primary/15 focus:border-networco-primary focus:bg-white dark:focus:bg-gray-950 transition-all outline-none text-networco-text dark:text-white font-medium placeholder:text-networco-authority-light/50" placeholder="Ola" />
                    </div>
                    <div>
                        <label for="lastName" class="block text-xs font-bold text-networco-authority-light/80 uppercase tracking-widest mb-2 ml-1">Etternavn</label>
                        <input type="text" id="lastName" name="lastName" required
                            class="w-full px-5 py-4 bg-networco-baby-50 dark:bg-gray-900 border border-networco-border dark:border-gray-700 rounded-2xl focus:ring-4 focus:ring-networco-primary/15 focus:border-networco-primary focus:bg-white dark:focus:bg-gray-950 transition-all outline-none text-networco-text dark:text-white font-medium placeholder:text-networco-authority-light/50" placeholder="Nordmann" />
                    </div>
                </div>

                <div>
                    <label for="email" class="block text-xs font-bold text-networco-authority-light/80 uppercase tracking-widest mb-2 ml-1">E-post</label>
                    <input type="email" id="email" name="email" required
                        class="w-full px-5 py-4 bg-networco-baby-50 dark:bg-gray-900 border border-networco-border dark:border-gray-700 rounded-2xl focus:ring-4 focus:ring-networco-primary/15 focus:border-networco-primary focus:bg-white dark:focus:bg-gray-950 transition-all outline-none text-networco-text dark:text-white font-medium placeholder:text-networco-authority-light/50" placeholder="ola@@networco.dev" />
                </div>

                <div class="space-y-6">
                    <div>
                        <label for="password" class="block text-xs font-bold text-networco-authority-light/80 uppercase tracking-widest mb-2 ml-1">Passord</label>
                        <input type="password" id="password" name="password" required minlength="@Model.MinPasswordLength"
                            class="w-full px-5 py-4 bg-networco-baby-50 dark:bg-gray-900 border border-networco-border dark:border-gray-700 rounded-2xl focus:ring-4 focus:ring-networco-primary/15 focus:border-networco-primary focus:bg-white dark:focus:bg-gray-950 transition-all outline-none text-networco-text dark:text-white font-medium placeholder:text-networco-authority-light/50" placeholder="••••••••" />

                        <!-- Strength Indicator -->
                        <div class="mt-4 space-y-3 px-1">
                            <div class="flex gap-1.5 h-1.5">
                                <div id="strength-1" class="flex-1 rounded-full bg-gray-100 dark:bg-gray-700 transition-colors"></div>
                                <div id="strength-2" class="flex-1 rounded-full bg-gray-100 dark:bg-gray-700 transition-colors"></div>
                                <div id="strength-3" class="flex-1 rounded-full bg-gray-100 dark:bg-gray-700 transition-colors"></div>
                                <div id="strength-4" class="flex-1 rounded-full bg-gray-100 dark:bg-gray-700 transition-colors"></div>
                            </div>
                            <p id="strength-text" class="text-[10px] font-bold uppercase tracking-wider text-gray-400 dark:text-gray-500">Styrke: Ingen</p>
                        </div>

                        <!-- Real-time Requirements (rendered from the server password policy) -->
                        <ul class="mt-4 space-y-2 px-1">
                            <li id="req-length" class="flex items-center gap-3 text-[11px] font-bold uppercase tracking-wider text-gray-400 transition-colors">
                                <span class="status-icon w-4 h-4 rounded-full border-2 border-current flex items-center justify-center text-[8px]"></span> Minst @Model.MinPasswordLength tegn
                            </li>
                            @if (Model.RequireUppercase)
                            {
                                <li id="req-upper" class="flex items-center gap-3 text-[11px] font-bold uppercase tracking-wider text-gray-400 transition-colors">
                                    <span class="status-icon w-4 h-4 rounded-full border-2 border-current flex items-center justify-center text-[8px]"></span> Stor bokstav
                                </li>
                            }
                            @if (Model.RequireLowercase)
                            {
                                <li id="req-lower" class="flex items-center gap-3 text-[11px] font-bold uppercase tracking-wider text-gray-400 transition-colors">
                                    <span class="status-icon w-4 h-4 rounded-full border-2 border-current flex items-center justify-center text-[8px]"></span> Liten bokstav
                                </li>
                            }
                            @if (Model.RequireDigit)
                            {
                                <li id="req-digit" class="flex items-center gap-3 text-[11px] font-bold uppercase tracking-wider text-gray-400 transition-colors">
                                    <span class="status-icon w-4 h-4 rounded-full border-2 border-current flex items-center justify-center text-[8px]"></span> Minst ett tall
                                </li>
                            }
                            @if (Model.RequireNonAlphanumeric)
                            {
                                <li id="req-special" class="flex items-center gap-3 text-[11px] font-bold uppercase tracking-wider text-gray-400 transition-colors">
                                    <span class="status-icon w-4 h-4 rounded-full border-2 border-current flex items-center justify-center text-[8px]"></span> Minst ett spesialtegn
                                </li>
                            }
                        </ul>
                    </div>
                    <div>
                        <label for="confirmPassword" class="block text-xs font-bold text-networco-authority-light/80 uppercase tracking-widest mb-2 ml-1">Gjenta passord</label>
                        <input type="password" id="confirmPassword" name="confirmPassword" required minlength="@Model.MinPasswordLength"
                            class="w-full px-5 py-4 bg-networco-baby-50 dark:bg-gray-900 border border-networco-border dark:border-gray-700 rounded-2xl focus:ring-4 focus:ring-networco-primary/15 focus:border-networco-primary focus:bg-white dark:focus:bg-gray-950 transition-all outline-none text-networco-text dark:text-white font-medium placeholder:text-networco-authority-light/50" placeholder="••••••••" />
                        <p id="match-text" class="text-[11px] font-bold uppercase tracking-wider text-gray-400 dark:text-gray-500 mt-3 px-1 flex items-center gap-3 transition-colors" style="visibility: hidden;">
                            <span id="match-icon" class="w-4 h-4 rounded-full border-2 border-current flex items-center justify-center text-[8px]"></span> Passordene må være like
                        </p>
                    </div>
                </div>

                <div id="clientErrorMessage" class="hidden bg-red-50 dark:bg-red-900/20 border border-red-100 dark:border-red-800/30 text-red-700 dark:text-red-400 rounded-2xl p-4 text-sm font-medium">
                </div>

                <div class="bg-networco-baby-50 dark:bg-indigo-900/10 border border-networco-primary/20 dark:border-indigo-800/20 rounded-2xl p-5">
                    <div class="flex gap-3">
                        <span class="text-xl">📧</span>
                        <p class="text-[11px] font-bold uppercase tracking-wider text-networco-primary-darker dark:text-indigo-400/60 leading-relaxed">
                            <strong>E-postbekreftelse</strong><br />
                            Vi sender en bekreftelseslenke for å sikre din identitet.
                        </p>
                    </div>
                </div>

                <button type="submit" class="w-full bg-networco-coral text-white font-bold py-4 px-6 rounded-2xl hover:bg-networco-coral-light hover:shadow-lg hover:shadow-networco-coral/30 transition-all duration-200 text-lg">
                    Opprett konto
                </button>
            </form>

            <div class="mt-10 text-center">
                <p class="text-sm font-medium text-networco-authority-light dark:text-gray-400">
                    Har du allerede en konto?
                    <a href="/Login?return_url=@System.Net.WebUtility.UrlEncode(Model.ReturnUrl)" class="text-networco-primary dark:text-networco-baby-200 hover:text-networco-primary-dark dark:hover:text-white font-bold ml-1">Logg inn i stedet</a>
                </p>
            </div>
        }

        <div class="mt-10 pt-8 border-t border-networco-border dark:border-gray-700">
            <p class="text-[10px] leading-relaxed text-center text-networco-authority-light/70 dark:text-gray-500 font-medium uppercase tracking-wider">
                Sikker registrering fra Networco.@if (Model.IduraEnabled)
                {
                    <text> Du kan også bruke BankID for rask tilgang.</text>
                }
            </p>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        const passwordInput = document.getElementById('password');
        const confirmInput = document.getElementById('confirmPassword');
        const registerForm = document.getElementById('registerForm');
        const errorDiv = document.getElementById('clientErrorMessage');

        // Requirements mirror the server password policy exactly (rendered from config).
        // Keys/labels here must stay in sync with the <li> items above.
        // The regexes use Unicode property escapes so they match the server's
        // Unicode-aware checks (char.IsUpper/IsLower/IsDigit/IsLetterOrDigit) — an
        // ASCII-only regex would false-green 'ø' as special or reject 'Å' as upper.
        const requirements = {
            length: { el: document.getElementById('req-length'), check: (p) => p.length >= @Model.MinPasswordLength },
        @if (Model.RequireUppercase)
        {
            <text>upper: { el: document.getElementById('req-upper'), label: 'én stor bokstav', check: (p) => /\p{Lu}/u.test(p) },</text>
        }
        @if (Model.RequireLowercase)
        {
            <text>lower: { el: document.getElementById('req-lower'), label: 'én liten bokstav', check: (p) => /\p{Ll}/u.test(p) },</text>
        }
        @if (Model.RequireDigit)
        {
            <text>digit: { el: document.getElementById('req-digit'), label: 'ett tall', check: (p) => /\p{Nd}/u.test(p) },</text>
        }
        @if (Model.RequireNonAlphanumeric)
        {
            <text>special: { el: document.getElementById('req-special'), label: 'ett spesialtegn', check: (p) => /[^\p{L}\p{Nd}]/u.test(p) },</text>
        }
        };

        const strengthBars = [
            document.getElementById('strength-1'),
            document.getElementById('strength-2'),
            document.getElementById('strength-3'),
            document.getElementById('strength-4')
        ];
        const strengthText = document.getElementById('strength-text');
        const matchText = document.getElementById('match-text');
        const matchIcon = document.getElementById('match-icon');

        function updateValidation() {
            const password = passwordInput.value;
            const confirm = confirmInput.value;
            let metCount = 0;

            // Update requirements list
            for (const key in requirements) {
                const req = requirements[key];
                const isMet = req.check(password);
                if (isMet) {
                    req.el.classList.remove('text-gray-400', 'text-red-500');
                    req.el.classList.add('text-green-600');
                    req.el.querySelector('.status-icon').textContent = '✓';
                    req.el.querySelector('.status-icon').classList.remove('border-current');
                    req.el.querySelector('.status-icon').classList.add('bg-green-600', 'border-green-600', 'text-white');
                    metCount++;
                } else {
                    req.el.classList.remove('text-green-600', 'text-red-500');
                    req.el.classList.add('text-gray-400');
                    req.el.querySelector('.status-icon').textContent = '';
                    req.el.querySelector('.status-icon').classList.add('border-current');
                    req.el.querySelector('.status-icon').classList.remove('bg-green-600', 'border-green-600', 'text-white');
                }
            }

            // Update strength indicator
            const strength = calculateStrength(password, metCount);
            updateStrengthUI(strength);

            // Update match indicator — only show when user has started typing in confirm field
            if (confirm) {
                matchText.style.visibility = 'visible';
                if (password === confirm) {
                    matchText.classList.remove('text-gray-400', 'text-red-500');
                    matchText.classList.add('text-green-600');
                    matchIcon.textContent = '✓';
                    matchIcon.classList.remove('border-current', 'bg-red-500', 'border-red-500');
                    matchIcon.classList.add('bg-green-600', 'border-green-600', 'text-white');
                    matchText.lastChild.textContent = ' Passordene er like';
                } else {
                    matchText.classList.remove('text-gray-400', 'text-green-600');
                    matchText.classList.add('text-red-500');
                    matchIcon.textContent = '✕';
                    matchIcon.classList.remove('border-current', 'bg-green-600', 'border-green-600');
                    matchIcon.classList.add('bg-red-500', 'border-red-500', 'text-white');
                    matchText.lastChild.textContent = ' Passordene må være like';
                }
            } else {
                matchText.style.visibility = 'hidden';
                matchText.classList.remove('text-green-600', 'text-red-500');
                matchText.classList.add('text-gray-400');
                matchIcon.textContent = '';
                matchIcon.classList.add('border-current');
                matchIcon.classList.remove('bg-green-600', 'border-green-600', 'bg-red-500', 'border-red-500', 'text-white');
            }
        }

        function calculateStrength(p, metCount) {
            if (!p) return 0;

            let score = metCount;
            if (p.length > @Model.MinPasswordLength + 4) score++;
        @if (!Model.RequireNonAlphanumeric)
        {
            // Reward symbols only when a special char isn't already a required (and
            // thus already counted in metCount) — avoids double-counting the same char.
            <text>if (/[^\p{L}\p{Nd}]/u.test(p)) score++;</text>
        }

            if (p.length < @Model.MinPasswordLength) return 1;

            if (score <= 2) return 1;
            if (score === 3) return 2;
            if (score === 4) return 3;
            return 4;
        }

        function updateStrengthUI(strength) {
            const colors = ['bg-gray-100', 'bg-red-500', 'bg-orange-500', 'bg-yellow-500', 'bg-green-500'];
            const labels = ['Ingen', 'Svakt', 'Middels', 'Godt', 'Sterkt'];

            strengthBars.forEach((bar, i) => {
                bar.className = `flex-1 rounded-full transition-colors ${i < strength ? colors[strength] : 'bg-gray-100 dark:bg-gray-700'}`;
            });

            strengthText.textContent = `Styrke: ${labels[strength]}`;
            strengthText.className = `text-[10px] font-bold uppercase tracking-wider ${strength > 0 ? colors[strength].replace('bg-', 'text-') : 'text-gray-400 dark:text-gray-500'}`;
        }

        passwordInput.addEventListener('input', updateValidation);
        confirmInput.addEventListener('input', updateValidation);

        registerForm.addEventListener('submit', function (e) {
            const password = passwordInput.value;
            const confirm = confirmInput.value;

            let error = '';

            if (password.length < @Model.MinPasswordLength) {
                error = `Passordet må være minst @Model.MinPasswordLength tegn`;
            } else {
                // Collect every unmet complexity requirement (excluding length, handled above)
                // so the message lists exactly what's missing — including spesialtegn.
                const missing = [];
                for (const key in requirements) {
                    if (key === 'length') continue;
                    if (!requirements[key].check(password)) missing.push(requirements[key].label);
                }
                if (missing.length > 0) {
                    error = 'Passordet må inneholde minst ' + missing.join(', ');
                } else if (password !== confirm) {
                    error = 'Passordene er ikke like';
                }
            }

            if (error) {
                e.preventDefault();
                errorDiv.textContent = error;
                errorDiv.classList.remove('hidden');

                // Highlight failed requirements in red
                for (const key in requirements) {
                    const req = requirements[key];
                    if (!req.check(password)) {
                        req.el.classList.add('text-red-500');
                    }
                }
            } else {
                errorDiv.classList.add('hidden');
            }
        });

        // Custom Norwegian validation messages for HTML5 popups
        const inputs = document.querySelectorAll('input[required], input[type="email"]');
        inputs.forEach(input => {
            input.addEventListener('invalid', function (e) {
                input.setCustomValidity(''); // Reset first
                if (input.validity.valueMissing) {
                    input.setCustomValidity('Vennligst fyll ut dette feltet');
                } else if (input.validity.typeMismatch && input.type === 'email') {
                    input.setCustomValidity('Vennligst oppgi en gyldig e-postadresse');
                } else if (input.validity.tooShort) {
                    input.setCustomValidity('Vennligst bruk minst ' + input.minLength + ' tegn');
                }
            });
            input.addEventListener('input', function (e) {
                input.setCustomValidity('');
            });
        });
    </script>
}
